# common

`common` contains shared business logic, utility functions, and types that are utilized across both the backend services and potentially the frontend applications in the `our_places_rs` monorepo.
